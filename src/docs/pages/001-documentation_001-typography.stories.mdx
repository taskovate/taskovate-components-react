import { Button } from '@components/core';
import { Meta, Story, Preview, Props } from "@storybook/addon-docs/blocks";
import { HeadingsShowcase } from '@components/docs';

<Meta 
  title="Documentation/Typography" 
  parameters={{ options: { isToolshown: false } }} 
/>

# Preview

<Preview>
  <Story name="Typography">
    <h1>Typography</h1>
    <h2>Typefaces</h2>
    <p>Recursive is the primary font face. It is a variable font.</p>
    <p>Regular text is using the default variable font values.</p>
    <p>Headings are set with values that give a slight slant and a slight casual value.</p>
    <p>Code is set with a value that give monospace spacing.</p>
    <h2>Headings</h2>
    <HeadingsShowcase />
    <h2>Other</h2>
    <p>Paragraph</p>
    <p><b>Bold</b></p>
    <p><a href="/">Link</a></p>
    <code>Paragraph var code = 1;</code>
    <pre>var code = 1;</pre>
  </Story>
</Preview>
